<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match docker.django>
  @type stdout
</match>

<match docker.worker>
  @type stdout
</match>

<match docker.worker_py3_8>
  @type stdout
</match>

<match docker.nodejs>
  @type stdout
</match>

<match docker.db>
  @type stdout
</match>

<match docker.sqs>
  @type stdout
</match>

<match docker.statsd-exporter>
  @type stdout
</match>